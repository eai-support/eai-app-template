# Traceability

Requirements are defined by the [Issue #3503 hardening amendment](https://github.com/enterpriseaigroup/Issues2025/issues/3503#issuecomment-5826177803).

| Requirement      | Implementation                                                                                                                                                                                                     | Owned evidence                                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| DTE-018          | recursive source-only, no-follow configuration manifest that rejects links in entries and every ancestor plus nonregular entries, then rechecks ancestors before reading; canonical provenance anchors             | nested/generated-output/dangling-link/linked-ancestor/FIFO/provenance tests                                     |
| DTE-020, DTE-021 | `.github/workflows/eai-app.yml` plus shared source-mode, canonical app-key, template-environment, path, signed-grant, and empty optional-alias handling                                                            | workflow assertions, demo compatibility, alias compatibility, and both source modes' negative tests             |
| DTE-022, DTE-024 | hidden artifact upload, no-follow nonempty archive check, confined no-link image-context and artifact trees, exclusive evidence creation, and pinned GitHub provenance attestation                                 | workflow assertions and build-root/archive/evidence link, path, existing-file, and empty tests                  |
| DTE-023          | immutable workflow action SHAs and Node OCI digest                                                                                                                                                                 | workflow and image-context assertions                                                                           |
| DTE-025          | credential-free lifecycle/build job, post-build source and output-tree checks, single-line GitHub outputs, then isolated OIDC handoff with exact nonce revalidation from immutable artifacts and step-local inputs | two-job permission, ordering, integrity, linked-build-root, nonce-binding, output-injection, and endpoint tests |
| DTE-026          | additive aliases plus optional legacy `source-unknown` target tenant and strict `eai-cli-generated` target binding                                                                                                 | compatibility and conflict tests                                                                                |
| DTE-086–DTE-088  | current-main merge; producer candidate remains unversioned until its automatic release                                                                                                                             | exact-head validation above and release-version tests                                                           |
